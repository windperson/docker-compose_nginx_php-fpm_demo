web:
  build: ./nginx
  links:
    - phpfpm
  volumes_from:
    - phpfpm
  ports:
    - "80:80"

phpfpm:
  build: ./phpfpm
  volumes:
    - phpfpm/config:/usr/local/etc
    - phpfpm/src:/var/www/html
  ports:
    - "9000:9000"
